{ // add: array case; ||; in expr part of if expr stmt (and similar) 
    int a = 1;
    int b = 0;

    a++;
    a;
    --b;

    a = a++ + (++b) - a++ - b++ + --a + --b + --b;

    if ( (0 == a) )
	a++;
    else if ( (7 == a) )
	--a;
    else
    a += ++b + (++a) + (b++) + a++;

}
