{ // add: array case, when ready
    int a = 1;
    int b = 0;

    a++;
    a;
    --b;

    a = a++ + (++b) - a++ - b++ + --a + --b + --b;

    if ( (0 == a++) || (++a && (b -1)) )
	a++;
    else if ( (0 == a++) && (++a || (b -1)) )
	a++;
    else if ( a = b++ - b++ + --b + --b && ++b)
	--a;
    else if ( a *= b += -b++ + --b + --b && ++b)
	--a;
    else
	a += ++b + (++a) + (b++) + a++;
}
